---
title: Concurrent requests
---

Ollama serves a fixed number of requests at once per model, set by
`OLLAMA_NUM_PARALLEL`, and it reserves that many slots' worth of KV cache when
the model loads — whether or not anyone uses them. Everything past that number
queues.

So you have to guess the number in advance. Too low and the card sits idle while
requests wait. Too high and you have spent on slots nobody opened the memory the
model itself needed, and the model may not even fit.

xollama removes the guess.

## What changes

With **dynamic slots**, the cache becomes one shared pool instead of a fixed
split, slots are allocated but parked, and the engine admits another only while
there is still room and speed for it. Nothing is reserved for a slot nobody is
using.

It is **on by default** and you do not have to do anything. One conversation can
use the whole context; several short ones run side by side; a fourth arriving
when there is no room waits rather than failing.

<Note>
  The total number of cache cells does not change. Ollama sizes the cache as
  context × parallel, and the engine allocates that same total whether the cells
  are one shared pool or a fixed split. What changes is whether one conversation
  may use all of them, or only its own fixed share.
</Note>

## What the ceiling costs

Almost nothing, on most models — and that is the point. A shared pool of four
slots holds the same cells as the single slot it replaced, so raising the
ceiling from one to four buys you three more concurrent conversations without
buying more memory.

"Almost" is doing some work in that sentence, and it is worth knowing where.

A **sliding-window model** — Gemma-3, Gemma-4 and their relatives — keeps a
second, much shorter cache beside the main one, holding just the recent window
for each conversation. That one is *not* shared. Every slot the ceiling allows
gets its own window, so a ceiling of four makes the short cache roughly four
times larger, whether or not four conversations ever arrive.

It is bounded: the short cache can never grow past one full context, so the most
a raised ceiling can cost you is one extra context's worth of cache. But on a
model with a large window it is real memory, and it is spent at load time.

xollama accounts for this when it decides where a model fits and what to evict.
The figure it plans against is the **ceiling**, not the slot the model starts on
— otherwise the plan would describe only the first request, and would go stale
the moment the engine admitted a second slot and quietly grew into memory that
had already been promised to something else.

Two practical consequences:

- On a model with no sliding window, raise the ceiling freely. It costs nothing
  at load and the engine's own brakes decide the rest.
- On a sliding-window model, a high ceiling is a real memory decision. Raise it
  in steps and watch what the load actually allocates.

### Sliding-window models: the one lever that helps

On a sliding-window model the reservation is per **sequence**, and both a slot
and a pool are sequences. At a ceiling of four with two pools that is six
windows reserved up front.

```dockerfile
XOLLAMA {"version": 1, "slots": {"dynamic": true, "max": 32, "swa_seq_budget": 8}}
```

`swa_seq_budget` sizes that short cache for fewer sequences than exist. It is
sound rather than a fudge: a window's cells become reclaimable once their token
falls out of the sequence that owned it, so eight windows serve rather more than
eight short conversations. What you give up is worst-case headroom — a sustained
load where every conversation stays at full window length will run the pool out.

It is worth setting where the sequence count is large. At a ceiling of four it
saves little and risks something, so leave it alone there. The memory xollama
plans for follows the budget, so setting it actually frees the memory rather
than merely telling the engine to use less of what was already reserved.

## Setting it

<Tabs>
<Tab title="Server">
```ini
# /etc/systemd/system/ollama.service.d/xollama.conf
[Service]
Environment="XOLLAMA_MAX_PARALLEL=8"
Environment="XOLLAMA_SLOTS_TPS_FLOOR=15"
```
</Tab>
<Tab title="One model">
```dockerfile
FROM gemma4:e4b

XOLLAMA {"version": 1, "slots": {"max": 8, "tps_floor": 15}}
```
</Tab>
</Tabs>

| Setting | Environment | Modelfile | Default |
|---|---|---|---|
| On or off | `XOLLAMA_DYNAMIC_SLOTS` | `slots.dynamic` | on |
| Ceiling | `XOLLAMA_MAX_PARALLEL` | `slots.max` | 4 |
| Speed floor | `XOLLAMA_SLOTS_TPS_FLOOR` | `slots.tps_floor` | unset |
| Memory reserve | `XOLLAMA_SLOTS_VRAM_RESERVE` | `slots.vram_reserve_mib` | engine default |

The model's setting wins over the server's, as everywhere else.

**The speed floor** is the more useful of the two brakes. It is the decode rate,
in tokens per second, that each request should keep: another slot is not
admitted if the projected rate would fall below it. Set it to the slowest you
would accept and the server stops adding concurrency at that point instead of
making everyone slower.

**The memory reserve** is free VRAM, in MiB, that must remain after admitting a
slot. Set it when something else shares the card.

## Turning it off

```shell
XOLLAMA_DYNAMIC_SLOTS=0
```

or `{"slots": {"dynamic": false}}` for one model. That restores Ollama's fixed
split exactly: `OLLAMA_NUM_PARALLEL` slots, each with its own share of the
cache.

Worth doing for a model where predictable per-request speed matters more than
throughput — a fixed split cannot be slowed down by a neighbour, because there
is no neighbour.

## Things that are decided for you

**A model that must be served one at a time is never grown.** Some
architectures give wrong answers with more than one sequence in flight, and
Ollama already refuses to run them in parallel. Dynamic slots respect that: a
model on that list stays at one slot no matter what you set, because that is a
correctness decision and not a capacity preference. Embedding models are the
same.

**A busy engine waits rather than failing.** When there is no room, the engine
refuses the request immediately rather than queueing it — which is right for the
engine and wrong to hand a caller, because a busy Ollama server is supposed to
queue. xollama waits it out for you, honouring the delay the engine asks for and
giving up if your own request times out first. A client that has never heard of
any of this does not start seeing errors because a server default changed.

If a server stays full for two minutes, the wait ends with an error saying so —
that usually means the context length or the slot ceiling is too large for the
memory available, not that the server is busy.

**Stock `llama.cpp` is unaffected.** Parking and admitting slots is something
the opencoti engine does; on stock the setting is inert and the launch is
Ollama's own, unchanged.

<Note>
  Dynamic slots compose with the opencoti engine's other cache features. The
  **rolling-KV window** — the one that spills cache to host RAM instead of
  failing — runs under the shared pool as well as the per-slot split, measured
  byte-identical across both. **PolyKV** prefix pools work on either layout too,
  sharing the prefix without copying it under the shared pool and with a
  physical copy under the split. You do not have to choose.
</Note>

<Warning>
  **A model too large for your VRAM still aborts on the engine xollama ships.**
  Measured 2026-09-20 on `opencoti-llamafile-0.10.5-c7` r2: a 39.2 GiB model on
  a 24 GiB card fails in under three seconds with
  `ggml_new_object: not enough space in the context's memory pool`, while the
  engine is placing KV cache layers on the CPU. Stock llama.cpp loads the same
  model on the same card, and the same engine serves a model that *fits* at
  full speed — so it is the partial offload specifically.

  It is the rolling-KV **position-window** tactic specifically, which
  `--kv-residency-mode auto` picks only under real VRAM pressure. Forcing the
  other tactic is measured to work, and is the fix to reach for first:

  ```shell
  LLAMA_ARG_KV_RESIDENCY_MODE=head xollama serve
  ```

  That loaded the same 39.2 GiB model on the same 24 GiB card at 2.85 tok/s —
  faster than stock llama.cpp managed on the same arm. Failing that, keep the
  load resident: a smaller model or quantisation, a lower `num_ctx`, or a
  compressed [KV cache type](/xollama/kv-cache); `XOLLAMA_ENGINE=llamacpp`
  serves it too. xollama recognises this abort and says all of this rather than
  leaving you the raw message.

  A *second*, separate crash — a failed assertion in the attention kernel as
  soon as the rolling-KV window spilled to host RAM — was real in the first c7
  cut and **is** fixed in the r2 build that ships (opencoti's patch 0253). The
  two were reported to us as one bug; retaking the measurement is what showed
  they are not. Numbers for both: [Phase 2](https://github.com/mann1x/xollama/blob/main/docs/evaluations/phase2-engine-ab.md).
</Warning>
