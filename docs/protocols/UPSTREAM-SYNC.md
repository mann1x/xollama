# Protocol — Upstream Sync

xollama is a **soft fork of [ollama](https://github.com/ollama/ollama)**. It
exists to carry fixes and features upstream has not merged, and to serve GGUF
models on the [opencoti-llamafile](https://huggingface.co/ManniX-ITA/opencoti-llamafile)
engine instead of stock llama.cpp.

Staying cheap to sync is the whole design constraint. Everything below exists
to keep `git merge upstream/main` a review, not an archaeology dig.

## Remotes

| Remote | Repository | Role |
|---|---|---|
| `origin` | `mann1x/xollama` | this fork |
| `upstream` | `ollama/ollama` | source of truth for the base |
| `fork` | `mann1x/ollama` | where PRs to upstream are staged |

`main` was seeded from upstream `v0.34.2` (`dfabde45`, 2026-09-17) **with the
full 5771-commit history**, deliberately. A squashed import has no merge-base,
and every later sync then needs a `git replace --graft` trick to find one —
opencoti had to do exactly that once, and once was enough. Here the merge-base
is real from day one.

## The rule

> **Every xollama change is either an additive file outside the upstream tree,
> or a surgical hook inside it.**

### Additive (preferred)

A file that does not exist upstream. New files live in:

- `docs/protocols/`, `docs/features/` (this tree)
- `llm/engine/` — the engine resolver and the opencoti adapter
- top-level identity files we own: `README.md`, `CLAUDE.md`, `AGENTS.md`

Additive files never conflict on an upstream merge.

### Surgical hook

A small edit to an existing upstream file, allowed **only** when there is no
additive way to get the effect. A surgical hook MUST:

1. Be the minimum-line edit that achieves it — one import plus one call site,
   or one conditional.
2. Carry a marker comment on the line above:
   `// xollama-hook: <feature-id> — see docs/features/<plan>.md`
3. Be inert when the feature is off. With `XOLLAMA_ENGINE=llamacpp` and no
   xollama flags set, behaviour must be byte-identical to upstream.
4. Be listed in the Registry below, in the same commit that adds it.

### What is NOT a surgical hook

- A refactor of an upstream file "while we're here". Send it upstream instead.
- A rename of an upstream symbol. Wrap, never rename.
- An unconditional behaviour change. Gate it.

## Why we do not rename the Go module

`github.com/ollama/ollama` stays the module path even though the product is
called xollama. This is measured, not sentimental:

- 547 `.go` files import that path, over 1238 import lines.
- Upstream touched 474 `.go` files between `v0.34.0` and `v0.34.2` alone —
  two patch releases, three weeks.
- **155 of those files are ones we would have rewritten.** A module rename
  buys a cosmetic import path and pays ~33% conflict rate on every sync,
  forever.

The binary, the CLI name, the app identity and the user-facing strings are
xollama's. The import path is upstream's. See
[`docs/features/rebrand.md`](../features/rebrand.md).

## Sync workflow

Sync on a branch, never on `main` — a half-resolved merge on the trunk is
hard to back out of.

```bash
git fetch upstream --tags
git rev-list --count upstream/main..main    # ours they do not have
git rev-list --count main..upstream/main    # theirs we do not have — the review size

git checkout main && git pull --ff-only
git checkout -b sync/upstream-YYYYMMDD
git merge upstream/main       # expect conflicts only where the tables say
```

Then, in order, and do not skip one because the previous passed:

1. `go build ./...` and `go vet ./...`
2. `go test ./llm/... ./server/... ./model/parsers/... ./thinking/...` —
   the carried patches live there and their tests are the guard.
3. `scripts/check-hooks.sh` — greps `xollama-hook:` markers and reconciles
   them against the Registry. A marker with no Registry row fails.
4. Re-check every carried PR in
   [`CARRIED-PATCHES.md`](./CARRIED-PATCHES.md): any that landed upstream
   gets **retired**, not merged twice.
5. Boot both engines against one model and compare:
   `XOLLAMA_ENGINE=llamacpp` vs `XOLLAMA_ENGINE=opencoti`.
6. Read `git diff main...HEAD` over the parser/thinking paths. Not the merge
   diff — the diff against our own trunk, which is what actually changed for us.

Merge with `--no-ff` so the sync is one identifiable commit range.

## Take gladly / read line by line

- **Take gladly.** GPU discovery, scheduler, registry/transfer, the app,
  build and CI, dependency bumps, new model architectures.
- **Read line by line.** `model/parsers/`, `thinking/`, `llm/llama_server.go`,
  `llm/server.go`, `server/routes.go`. This is precisely the surface the
  carried patches change *and* the surface upstream edits most. A merge that
  resolves cleanly is not evidence it resolved correctly — a parser fix
  upstream rewrote and a parser fix we wrote touch the same lines for
  unrelated reasons, and git will happily keep one of them.

## Workflow hooks

Hooks in `.github/workflows/*.yaml` carry a `# xollama-hook: <id>` comment, but
`scripts/check-hooks.sh` scans Go and TypeScript only, so they are listed here,
outside its table. After a sync, check them by hand:
`grep -rn 'xollama-hook' .github/`.

| Workflow hook | File | Feature |
|---|---|---|
| `docker-release` | `release.yaml` `darwin-build` and `latest.yaml`: guarded to `ollama/ollama`; `darwin-build` dropped from `release`'s `needs` | [features/docker-release.md](../features/docker-release.md) |
| `hosted-ci` | `test.yaml` `linux` and `windows` jobs: `runs-on` is upstream's self-hosted label on `ollama/ollama` and `ubuntu-latest` / `windows-2022` elsewhere (`windows-latest` and `windows-2025` both resolve to the `windows-2025-vs2026` image: CUDA 13.0 rejects its MSVC and ROCm 7.1's clang cannot compile its STL). The fork has no runners, and these legs compile the native presets and check the payload. They need no GPU. Upstream ran them on `ubuntu-latest` until `92b1a21f` (2024-02-06). The same id marks `fail-fast`, which is off on the fork: the legs are independent and hosted minutes are free for a public repo, so every leg reports. It also marks the Windows build step's Visual Studio lookup, which keeps upstream's `2022\Enterprise` path when it exists and otherwise asks `vswhere` for a 17.x (2022) install, because the hosted image does not have that path | [protocols/RELEASE.md](RELEASE.md) |
| `fork-release` | `release.yaml` `setup-environment`: guarded to `ollama/ollama`, which skips every job chained from it. A stray `v*` tag on the fork must not queue jobs on runners it does not have | [protocols/RELEASE.md](RELEASE.md) |
| `security-deps` | `go.mod` / `go.sum`: `golang.org/x/{crypto,image,net}` raised to their first fixed releases (v0.52.0, v0.45.0, v0.55.0) plus the `x/{sync,sys,mod,term,text}` versions those require; every changed line carries the marker. govulncheck (2026-09-25) found 6 of the 19 Dependabot `go.mod` alerts reachable from xollama code: `x/image` WebP/VP8L decoding of images sent to the API (3), `x/net` (2), `x/crypto` (1). The `go` line stays upstream's `1.26.0`: the fixed releases need only 1.25. **Retire** a line when upstream's `go.mod` reaches or passes it (take upstream's version, drop the marker); a sync that conflicts here resolves to the higher of the two versions. The release toolchain is the other half: `xollama-release.yaml` builds with upstream's Go line at its newest patch (go1.26.8 today), since go1.26.0 carries 20 reachable standard-library fixes; that file is fork-only and needs no hook. With both, govulncheck reports 0 reachable. | [protocols/RELEASE.md](RELEASE.md) |

## Registry — known surgical hooks

Add the row in the same commit as the hook.

| Hook ID | File | Feature | Plan |
|---|---|---|---|
| `default-port` | `envconfig/config.go` — `DefaultPort` replaces the `11434` literal in `Host()`, and `server/create.go`'s `remoteURL` resolves a bare host or path against it instead of upstream's port. xollama listens on **22434**: 11434 is IANA-registered to `ollama`, 22400-22499 is unassigned. This removes an accidental collision; it does **not** make side-by-side operation supported, and the docs say so. Everything else derives its endpoint from `envconfig.Host()` and moved for free — only the launcher tests that assert a rendered config changed. `TestTheDefaultPortIsNotUpstreams` pins the number, because every other test now references the constant and would follow it back to 11434. | Default port | [xollama/default-port.mdx](../xollama/default-port.mdx) |
| `host-namespace` | `envconfig/config.go` — `XollamaOnly` beside `XollamaKey`, called by `Host()` in place of `Var()`. The listen address is the only setting read from `XOLLAMA_HOST` **exclusively**; `OLLAMA_HOST` is not consulted for it. Without this the separate port protects nothing, since `OLLAMA_HOST` is exactly what a stock ollama install exports and would put both servers back on one socket. Every other `OLLAMA_*` var keeps its fallback — a cache type is shareable, an address is not. `TestTheListenAddressIsNotInheritedFromAStockOllama` is the only test in the tree that fails if `Host()` goes back to `Var()`; `TestOnlyTheListenAddressIsExclusive` holds the other direction. | Default port | [xollama/default-port.mdx](../xollama/default-port.mdx) |
| `host-fallback` | `api/xollama_identity.go`, `api/xollama_host.go` and `cmd/xollama_host.go` are additive and hold the whole decision; the hooks are two lines in `cmd/cmd.go` (`checkServerHeartbeat`, `versionHandler`) and the two `/api/xollama` route registrations in `server/routes.go`. With no `XOLLAMA_HOST` set the CLI probes **22434** and then **11434**, so an operator with a single-server install need not know the port moved. It resolves ONCE, from the `PreRunE` every server-using command already shares, and records the answer in the environment -- deliberately not in `api.ClientFromEnvironment`, which has ~20 call sites and must stay a pure function of the environment; probing there made `TestClientFromEnvironment` pass or fail on whether a server happened to be running. A **stock ollama** on 11434 is a refusal naming `XOLLAMA_HOST`, never a silent connection: `pull`, `rm` and `tweak` would otherwise act on the other server's store. Identification is `/api/xollama` first, then the fork's name in `/api/version` -- required, because every xollama built before that route 404s it exactly as a stock ollama does (measured here: `0.34.2-xollama-12644ae4` 404'd it). An explicit `XOLLAMA_HOST` is never probed and never overridden. This is the CONNECT address only; `serve` still binds `envconfig.Host()` and the `host-namespace` rule above is unchanged. Guarded by `api/xollama_host_test.go`. | Default port | [xollama/default-port.mdx](../xollama/default-port.mdx) |
| `store-ownership` | `internal/fsowner/` is additive and holds the decision; the hooks are one-word substitutions — `os.MkdirAll`/`WriteFile`/`Create`/`CreateTemp`/`OpenFile` become the `fsowner` wrappers — in the eleven files that write the shared installation: `manifest/{paths,manifest,layer}.go`, `server/{images,create,download,gguf_metadata,model_recommendations,inference_request_log}.go`, `internal/onboarding/state.go`, `x/transfer/download.go`, plus one `fsowner.Preflight` call in `server/routes.go`'s `Serve`. The wrappers decide from the DESTINATION, not the call site: a new file belongs to whoever owns the nearest existing ancestor directory, a root-owned ancestor is not evidence, and a non-root process gets no answer at all — so outside a service-owned tree they are `os` plus one stat. This exists because ollama on Linux is a system service: an administrator's root-run command would otherwise leave files the service cannot replace, which surfaced once as a model list taking 9.89 s instead of 0.06 s and was reported as a timeout, never as a permission error. `Preflight` covers the mirror case a chown cannot fix — an ordinary user running against a service-owned store — with a warning naming the owner and the repair. Windows is a no-op by build tag. Guarded by `internal/fsowner/fsowner_test.go` and `create_test.go`. | Installation ownership | [features/store-ownership.md](../features/store-ownership.md) |
| `engine-payload` | `llm/engine/payload.go` is additive and holds all of it. The hook is one block in `llm/llama_server.go`, inside `if usedOpencoti` just before `exec.Command`: it sets `HOME` for the subprocess to a private payload root — preferring `ml.LibOllamaPath`, where the rest of the engine runtime lives, and falling back to `~/.ollama` where that is not writable — so a self-extracting engine unpacks into a directory holding only its own payload. Stock `llama-server` extracts nothing and never reaches the block, so the off path keeps the environment it inherited. Never fatal — an unusable root warns and leaves `HOME` alone. On Linux it also refuses to create that directory as root: `internal/fsowner` (additive) takes the owner of the model store, since whoever owns the store is the service, and hands the directory over with setgid and group write so a purge still works on entries an earlier root run left. Non-root does nothing; Windows is a no-op by build tag. Guarded by `llm/engine/payload_test.go` and `internal/fsowner/fsowner_test.go`. | Engine payload isolation | [features/engine-opencoti-llamafile.md](../features/engine-opencoti-llamafile.md) |
| `engine-args` | `llm/engine_args.go` is additive and holds the whole hatch — the splitter, the guarded-flag table and the refusals. The hook is one call in `llm/llama_server.go`, in `startLlamaServer` immediately before `exec.Command` and after every other append, so the operator's arguments are last and win a repeat. With `XOLLAMA_ENGINE_ARGS` unset it returns the argv slice untouched, so the off path allocates nothing and logs nothing; `envconfig/config.go` gains `EngineArgs` and its `AsMap` row. Deliberately has no model-side carrier: `types/xollama` is not involved and must not become involved. Guarded by `llm/engine_args_test.go`. | Operator argv passthrough | [xollama/engine-args.mdx](../xollama/engine-args.mdx) |
| `opencoti-discover` | `discover/runner.go` — one line before the first-pass `libDirs` reset in `GPUDevices`: `devices = filterIntegratedGPUs(overlayOpencotiDevices(ctx, devices))`. For each backend opencoti would serve (`engine.Enumerates`: the tested matrix intersected with the pin, or a forced `XOLLAMA_ENGINE=opencoti`), the engine lists its own devices (`engine.EnumerateCommand`: `--server --list-devices -m /nonexistent.gguf --offline --verbose --gpu <backend>`) and becomes the source of truth for which devices exist, their indexes and their memory; identity (PCI ID, integrated) is joined from what llama.cpp and the native probe found, by name and then by position. The reason is that the two engines ship different ggml builds, and the index `GGML_VK_VISIBLE_DEVICES` selects by only means something to the engine that produced it: on solidPC both list the Renoir iGPU twice (RADV and amdvlk 20.40) in ICD order, which put RADV at 1 for opencoti when amdvlk's ICD came first. Code in `discover/opencoti.go`, `llm/engine/enumerate.go`. Under `XOLLAMA_ENGINE=llamacpp` nothing is run and the list is returned untouched (`TestOffMeansOffForDiscovery`). Runs once at bootstrap, never on a free-memory refresh. | Device selection | [features/device-selection.md](../features/device-selection.md) |
| `device-select` | `server/sched.go` — one block after `s.updateFreeSpace(gpus)` in the pending loop: unless `num_gpu` is 0, `selectModelDevices(pending.model.Xollama, gpus)` narrows the device list to the model's `devices` pin, or sends a refusal to `pending.errCh` and stops; `server/routes.go` — one `r.GET(api.XollamaDevicesPath, XollamaDevicesHandler)` beside the identity route. Code in `server/device_select.go` and `server/identity.go`, types in `api/xollama_devices.go` and `types/xollama/devices.go`. A model without a pin takes upstream's path unchanged except that an integrated Vulkan GPU is set aside while a discrete GPU is present — and under `XOLLAMA_ENGINE=llamacpp` no integrated Vulkan GPU is admitted at all, so off stays off. Guarded by `server/device_select_test.go` and `server/sched_device_select_test.go` (`TestSchedRefusesAModelWhosePinnedDeviceIsAbsent`, `TestSchedHandsLoadOnlyThePinnedDevices`). | Device selection | [features/device-selection.md](../features/device-selection.md) |
| `igpu-vulkan` | `discover/runner.go` — one `case "Vulkan": return true` in `integratedGPUAllowedByDefault`, admitting an integrated Vulkan device on the same terms upstream already gives an integrated CUDA one. Upstream drops it, and `discover/runner_test.go`'s `auto admits only allowlisted integrated GPUs` subtest asserts the drop, so that expectation carries the marker too. The reason is that Vulkan is not a fallback on this fork: opencoti ships its own Vulkan implementation and is the engine that serves it (`gpuFlag` → `--gpu vulkan`), and an integrated GPU is the case that path exists for — a small agentic model served at idle power without waking a discrete card. Hiding it by default made that case reachable only via `OLLAMA_IGPU_ENABLE=1`, which no model config can set. Measured before the change on solidPC against AMD RADV RENOIR (ACO), PCI `0000:18:00.0`, on the host Mesa 20.3 stack: `qwen3.5:2b` served **512 coherent tokens at 19.51 tok/s** with 2.47 GB resident on `Vulkan0`. `OLLAMA_IGPU_ENABLE` still overrides in both directions, and non-Vulkan integrated devices are untouched. Under `XOLLAMA_ENGINE=llamacpp` it returns false, as upstream does (`TestOffMeansTheIntegratedVulkanGPUIsHiddenAsUpstreamHidesIt`). A second marker in `server/routes.go` keeps an integrated Vulkan GPU out of the `defaultNumCtx` VRAM tier: it reports host RAM, and on solidPC counting it lifted the default context from 32768 to 262144. | Device selection | [features/device-selection.md](../features/device-selection.md) |
| `engine-select` | `llm/llama_server.go` — in `startLlamaServer`, immediately before `exec.Command`: `engine.Launch(exe, params, engineBackends(launch.gpus), ml.LibOllamaPath)`, plus the `llm/engine` import and the `engineBackends` helper just above `startLlamaServer`. Returns `(exe, params)` unchanged for llama.cpp, so the off path is byte-identical. `Launch` also returns whether it chose opencoti, which `startLlamaServer` passes back as a fourth result (one caller) so `Load` can offer the opt-in `XOLLAMA_ENGINE_FALLBACK` retry via `retryOnStockEngine`, modelled on upstream's own `retryWithMMProjCPUOffload`; `llamaServerLaunchConfig.forceStockEngine` skips the hook on that retry. | Engine — opencoti-llamafile | [features/engine-opencoti-llamafile.md](../features/engine-opencoti-llamafile.md) |
| `memory-scrape` | `llm/llama_server.go` — `bufferSizeRegex` accepts the per-stream KV shape (`KV buffer (stream N) size =`, printed whenever `-np > 1` without `--kv-unified`; dynamic slots do pass that flag, but only on an opencoti load that raised its ceiling, so both shapes still occur) and counts `KVarN` as KV; `memoryBufferKey` gains `stream` so streams sum instead of colliding. Without it a multi-slot load silently under-counts its KV — no warning fires, because the model and compute lines still match. Guarded by `TestMemoryParsingCountsPerStreamKVBuffers` and siblings. | Engine — opencoti-llamafile | [features/engine-opencoti-llamafile.md](../features/engine-opencoti-llamafile.md) |
| `env-namespace` | `envconfig/config.go` — `Prefix`, `XollamaKey`, the XOLLAMA_-first lookup in `Var`, the two `XOLLAMA_ENGINE*` rows and the `Name`-branding loop at the end of `AsMap`; `cmd/cmd.go` — the OLLAMA_-fallback note in `appendEnvDocs` and the `XOLLAMA_ENGINE*` entries in `serve`'s list. Seven call sites also moved from `os.Getenv("OLLAMA_…")` to `envconfig.Var` (`cmd/bench`, `app/cmd/app` ×2, `app/ui`, `app/store`, `mlx/dynamic.go`, `server/routes.go`); those carry no marker because `TestNoDirectReadsOfOurOwnEnvironment` in `envconfig/namespace_test.go` fails if a merge puts one back. | XOLLAMA_ environment namespace | [features/rebrand.md](../features/rebrand.md#the-xollama_-environment-namespace) |
| `draft-assistant` | `llm/llama_server.go` — `draftTypeAssistant` plus `specTypeArg`/`retargetSpecType`; `externalDraftType` selects it from the drafter's `requires_target_arch` and rejects a mismatched target; `appendDraftArgs` emits upstream's `draft-mtp` spelling, and the rewrite to `draft-assistant` happens only after `engine.Launch` says opencoti won, so the stock path is byte-identical. A gemma-4 drafter is a head with no context of its own and the two engines name that driver differently — upstream reaches it through `draft-mtp`, opencoti through `draft-assistant`. Guarded by `TestRetargetSpecType`, `TestExternalDraftType` and `TestAppendDraftArgsAssistantUsesUpstreamSpelling`. | Gemma-4 assistant drafters | [features/gemma4-drafter.md](../features/gemma4-drafter.md) |
| `modelfile-roundtrip` | `server/images.go` — one condition in `Model.String()`. `show --modelfile` is the documented way to derive a Modelfile from a model, so what it invents becomes a real layer on the rebuild. `GetModel` seeds every model with `template.DefaultTemplate`, so the `m.Template != nil` guard was always true and a model carrying NO template layer printed `TEMPLATE {{ .Prompt }}` — fed back to `create`, that bakes a 13-byte template layer into a model that never had one, permanently and invisibly changing how it is served (this is how a published vision tag in this owner's registry gained a `{{ .Prompt }}` layer its text sibling lacked). The gate is now `m.HasGoTemplate`, which is set in exactly one place — where an `image.prompt` / `image.template` layer is actually read — so it means precisely "the model defines one". `Model.String()` has one caller, so nothing else changes shape. Guarded by `server/modelfile_roundtrip_test.go`, which holds both halves: no `TEMPLATE` without a layer, and the stated template preserved when there is one. | Modelfile round-trip | [features/modelfile-roundtrip.md](../features/modelfile-roundtrip.md) |
| `model-config` | `types/xollama/` is additive and carries the schema; the hooks are the wiring. `api/types.go` — `CreateRequest.Xollama`; `parser/parser.go` — the `xollama` command in `isValidCommand` and `Command.String`, the `case "xollama"` in `CreateRequest` and the `parseXollamaConfig` helper; `create/manifest.go` — `ModelfileLayerOptions.Xollama` plus `appendXollamaConfigLayer`/`removeXollamaConfigLayer`; `server/create.go` — one field passed through; `server/images.go` — `Model.Xollama`, the `case xollama.MediaTypeImageJSON` that reads it back, and the `XOLLAMA` command `Model.String()` appends so `show --modelfile` stops being the one place a model's config is silently dropped (through `Config.Marshal`, so the printed schema version is the one a rebuild would store); `cmd/create_safetensors.go` — one `case "xollama"` in `configFromModelfile`, which runs on EVERY create and whose default branch was rejecting the documented directive as `unknown parameter 'xollama'` before the GGUF path was reached, plus `parser.ParseXollamaConfig` exported for it; `cmd/cmd.go` — a refusal naming `xollama tweak model`, because `create.PipelineOptions` carries no config layer and a safetensors import would drop an `XOLLAMA` line silently; `llm/server.go` — `LlamaServerConfig.Xollama` and its two accessors; `server/routes.go` — one field in `llamaServerConfigForModel`, filled with `LaunchConfig()` so a council (request-side only) never makes a runner differ; `llm/llama_server.go` — the engine pin at launch and the spec-type override (`resolveDraftType`, consulted for a built-in head as much as for an attached drafter: gating it on `DraftModelPath` made the pin a silent no-op on every model whose head ships inside the target); `api/types.go` — `ShowResponse.Xollama` and `server/routes.go`'s one line filling it, so a client can READ a model's config and not only write one (`xollama tweak model` needs the current setup before it can change one setting and leave the rest, and reading the manifest client-side would only work against a local store); `cmd/cmd.go` — two lines registering `tweak.Command` and listing its fallback environment variables, plus two `tableRender` blocks in `showInfo`: the model's stated settings (from `tweak.SettingRows`, so `show` and the wizard cannot name them differently) and its `Drafter` (from `api.ShowResponse.Drafter`, filled by the additive `server/drafter_show.go` out of the additive `llm/drafter.go`, so `show` and the launch cannot disagree about which `--spec-type` a model gets — a drafter is a manifest layer or a handful of tensors and appears nowhere else in the response), with the command itself additive in `cmd/tweak/`. The media type is upstream's own, so push, pull and blob GC carry the layer without knowing what it is; the layer NAME (`xollama.json`) is what separates it from upstream's `config.json`. A model with no config produces no layer and takes every upstream path unchanged. `create/manifest.go` reads the POINTER, not `IsZero`: nil means the request says nothing and the parent's layer is inherited, which is every ordinary create, while a present-but-empty config removes the layer -- `tweak model --clear` and a Modelfile stripping a parent's engine pin have no other way to say it. Guarded by `types/xollama/config_test.go`, `parser/xollama_test.go`, `create/xollama_test.go`, `cmd/tweak/*_test.go` and `TestLlamaServerConfigXollamaAccessorsTolerateNil`. | Model config carrier | [features/model-config.md](../features/model-config.md) |
| `council` | `internal/council/` (the runner: route-only decision, researchers and critics in parallel on `errgroup`, synthesizer) and `server/council.go` (members served in process through `ChatHandler`, each on its own engine session under the conversation's; deliberation streamed as thinking, the answer as content through `writeChatResponse`) are additive. `server/routes.go` — one `if councilServes(...)` in `ChatHandler`, after the remote-model branch and before capability checks. A model without an enabled council, a request with tools or a format, and every member's own turn (marked by a gin context key, never a header) take upstream's path unchanged. Guarded by `internal/council/council_test.go` and `server/council_test.go` (`TestAModelWithoutACouncilIsUntouched`). | Council chat | [plans/agentic-council-chat.md](../../plans/agentic-council-chat.md), [xollama/tweak.mdx](../xollama/tweak.mdx) |
| `engine-session` | `llm/engine_session.go` is additive and holds the whole resolver; the hooks are the wiring. `llm/server.go` — `SessionID`/`PoolID` on `CompletionRequest` and `ChatRequest`, plus the `sessionAffinity`/`sessionPool` accessors; `llm/llama_server.go` — `session_id`/`pool_id` on `llamaServerCompletionRequest` and in the chat body map, both written through `sessionFieldsFor`/`applySession`; `server/routes.go` — `sessionIDForRequest` and the four request construction sites that pass it. The engine gate is checked first and is absolute: on stock llama.cpp no field is written at all, so the request body is upstream's byte for byte. Shared prefix pools are the same hook: `llm/engine_pool.go` is additive and holds the registry, the key derivation, the shared-prefix construction (`/apply-template` + `/tokenize` + longest common token prefix) and the pool calls; `llm/server.go` gains `PoolKey` on both request types, `server/routes.go` a `poolKeyForRequest` beside `sessionIDForRequest` at the same three sites, plus `poolProbesForRequest`, which renders the prefix against `llm.PoolProbeContents` through ollama's own template and is carried on `CompletionRequest.PoolProbes` — necessary because on that path ollama, not the engine, owns the template, and a pool measured against the wrong renderer stops in the wrong place, `llm/llama_server.go` a `pools` field built in `startProcess`, the attach in both request paths, a deferred capture on success in `Completion`/`Chat` (both gained a named error result for it, and each supplies the rendering its own path owns), and `releasePools` at the head of `Close`. `llm/engine_pool_arch.go` is additive and classifies models that keep recurrent state; those are pooled only from the chat path, where `templateBoundary` can measure where the shared template ends, since the engine shares such a state only when the match covers the pool entirely. Guarded by `llm/engine_session_test.go` and `llm/engine_pool_test.go`. | Engine sessions and shared prefix pools | [xollama/sessions.mdx](../xollama/sessions.mdx) |
| `launch-config` | `llm/engine_launch.go`, `llm/engine_admission.go`, `llm/engine_estimate.go` and `llm/engine_dca.go` are additive and hold the resolvers; the hooks are the wiring. `llm/llama_server.go` — KV cache types emitted through `resolveKVCacheTypes`/`appendKVCacheArgs` instead of the single upstream type, the post-`engine.Launch` refusal for a type stock llama.cpp cannot parse, `appendKVCacheRingArgs` `appendSlotArgs` and `appendDCAArgs` (all after the engine is known, like `retargetSpecType`), the DCA refusals for a stock engine and for an architecture with no chunked route, `trainContext` on `llamaServerLaunchConfig`, the semaphore resize in `startProcess`, the relaunch at one sequence when a load the scheduler predicted for opencoti lands on stock (`servedSequences`), and the completion POST routed through `postWaitingForAdmission` so the engine's 429 becomes a wait rather than an error; `llm/server.go` — `SingleSequenceOnly` and `SingleSequenceStockOnly` on `LlamaServerConfig`, the `flashAttention` accessor, and the `DCAUnlocksContext` branch on upstream's `num_ctx` clamp in `NewLlamaServer`; `LlamaServerFlashAttention` gained a config argument so a model can override `OLLAMA_FLASH_ATTENTION` (a model that says nothing takes upstream's path byte for byte, guarded by `TestFlashAttentionWithoutAModelOpinionIsUpstreams`); `server/routes.go` — `parallelUnsafeArchitectures`/`singleSequenceOnly`/`parallelUnsafeArchitecture` and two fields in `llamaServerConfigForModel`; `server/sched.go` — `load()` calling `singleSequenceOnly` and applying the ollama/ollama#4165 cap only when `llm.WouldUseOpencoti` says stock llama.cpp will serve (the deny-list is stock's; with `XOLLAMA_ENGINE=llamacpp` the predictor is false and the cap is upstream's), `slotCeilingVRAM` added to the four `llm.PredictServerVRAM` call sites so the memory prediction is made against the slot ceiling rather than the slot the runner starts on, an `unlocked` argument on `effectiveModelContext`/`effectiveLlamaServerContext` with `LlmRequest.contextUnlocked` feeding it, and `runnerServesPastTrainedContext` guarding the clamp in `needsReload`. Every engine-specific flag is gated on opencoti, so a stock launch is upstream's argv unchanged. Guarded by `llm/engine_launch_test.go`, `llm/engine_slots_test.go`, `llm/engine_estimate_test.go`, `llm/engine_dca_test.go`, `server/sched_slot_estimate_test.go`, `server/sched_dca_test.go` and `server/sched_single_sequence_test.go`. | KV cache, dynamic slots, memory prediction, DCA | [xollama/kv-cache.mdx](../xollama/kv-cache.mdx), [xollama/slots.mdx](../xollama/slots.mdx), [xollama/dca.mdx](../xollama/dca.mdx) |
| `engine-introspect` | `llm/engine_introspect.go` and `server/routes_engine.go` are additive and hold the whole feature; the hooks are two lines. `server/routes.go` — `r.GET("/api/engine", s.EngineHandler)` beside `/api/ps`; `server/sched.go` — an `llama` field on the fork-local `loadedModel` struct and one assignment in `loadedModels`. `EngineIntrospector` is a deliberately OPTIONAL interface, type-asserted at the call site rather than added to `LlamaServer`: only the llama-server family has an HTTP surface, and a method on the upstream interface purely so the MLX runner can return "unsupported" is a method every merge has to carry. The endpoint whitelist (`props`, `slots`, `metrics`, `polykv/pools`) is validated before any connection is opened. Read-only, adds nothing to the inference path. Guarded by `llm/engine_introspect_test.go` and `server/routes_engine_test.go`. | Engine introspection | [xollama/introspection.mdx](../xollama/introspection.mdx) |
| `engine-defects` | `llm/engine_defects.go` is additive and holds the table and the matcher; the hook is one line in `llm/llama_server.go` — `Load` returns `s.annotateEngineDefect(err)` instead of a bare `err` on the final opencoti failure path. Diagnosis only: it never retries, downgrades or changes what was launched, and the original error is wrapped rather than replaced so anything downstream that inspects it keeps working. A row matches only when BOTH the artifact file name and the engine's dying words match — version alone would blame the build for unrelated failures, signature alone would blame a defect a newer cut has fixed. `TestKnownDefectsMatchThePinnedArtifact` fails when the pin moves past a listed build, which is the reminder to retire the row. Guarded by `llm/engine_defects_test.go`. | Known engine defects | [xollama/slots.mdx](../xollama/slots.mdx) |
| `app-state` | `app/cmd/app/app_windows.go` (`appPath`, `appLogPath`, `startupShortcut`, `shortcutOrigin`), `app/server/server_windows.go` (`pidFile`, `serverLogPath`), `app/wintray/menus.go` (`showLogs`), `app/store/store.go` (`legacyConfigPath`), `internal/onboarding/app_state.go` (`AppDatabasePath`) — the Windows app's own state moves from `%LOCALAPPDATA%\Ollama` to `%LOCALAPPDATA%\xOllama`, its install default to `Programs\xOllama`, and its login shortcut to `Startup\xOllama.lnk`, copied from the `lib\xOllama.lnk` the installer lays down. Found by the first real install (eleven2go, `v0.34.2-xollama.1` dry run): a stock server beside it held `Ollama\server.log` open, so `openRotatingLog` failed and the app's server never started, with nothing logged; `cleanup()` would also have stopped whatever process a stock app wrote into the shared `ollama.pid`, and the shared `db.sqlite` mixed both apps' settings. The login shortcut never worked: the app copied from a `lib\Ollama.lnk` the installer does not create, and skipped the copy whenever a stock `Startup\Ollama.lnk` existed. Settings are not migrated from the stock directory — they are the stock app's. `internal/onboarding/xollama_app_state_test.go` reads the source (the files are Windows-only) and fails on any stock path under `app/` or `internal/`; verified to flag all nine without the hook. | Windows installer and updates | [features/windows-installer.md](../features/windows-installer.md) |
| `update-feed` | `app/updater/fork.go` is additive and holds the whole feed; the hooks are two `if forkFeedActive()` guards in `app/updater/updater.go` — one at the head of `checkForUpdate`, one before `VerifyDownload` in `DownloadNewRelease`. Upstream asks `ollama.com/api/update`, signs the query with the user's own SSH key, and installs what comes back if it is Authenticode-signed by `Ollama Inc.`; on a fork that endpoint answers for the wrong product and the signer check **passes** for stock ollama and **fails** for us, so left alone it replaces xollama with upstream on an hourly timer. The fork reads its own GitHub releases instead, sends nothing identifying, picks the release here rather than letting a server pick (every release starts `--draft --prerelease`, which `/releases/latest` would never see), and gates the download on the sha256 that same release published — the check that tells our installer from another product's, which a signature cannot. The guard is `UpdateCheckURLBase == upstreamUpdateCheckURL` rather than a separate switch so upstream's own tests, which point that variable at a local server, keep exercising the endpoint protocol unchanged; a shipped build never moves it. A third hook in `app/updater/updater_windows.go` demotes the `Ollama Inc.` signer requirement to advisory and points the allowlist at our own organisation — restore it to a hard requirement, with our organisation, the day the fork has a certificate. Two more there move the update staging dir, the upgrade log and the marker from `%LOCALAPPDATA%\Ollama` to `%LOCALAPPDATA%\xOllama` and drop upstream's sweep of `Ollama\updates`, which on this fork would delete a stock install's staged update. Delta updates ride the same file: `chooseCoreInstaller` takes the executables-only installer (`xOllamaUpdate.exe`, ~36 MB against ~1.5 GB) when the payload digest the full installer wrote to `lib\ollama\PAYLOAD_ID` matches the release's `payload-id.txt`, and every way of not knowing falls back to the full installer. `app/updater/fork_payload_windows.go` is additive and reads the marker. One more hook in `app/cmd/app/app.go` accepts `xollama://` beside `ollama://`, because the installer no longer CLAIMS `ollama://` — that key belongs to a stock install, and `uninsdeletekey` on it meant uninstalling xollama broke a working ollama's links. Guarded by `app/updater/fork_test.go`. The same id marks three upstream Windows tests that these hooks invalidate. They were first run on 2026-09-25, when `test.yaml` reached hosted runners. `TestVerifyDownloadRejectsUnsignedWindowsInstaller` and `TestDoUpgradeAtStartupRejectsUnsignedWindowsInstaller` skip, because the fork channel is unsigned; `fork_signer_windows_test.go` asserts the acceptance instead. `TestLiveAppUpdate` (`updater_live` tag) reads the fork's feed with pre-releases allowed, and skips only while the fork has published no release with an installer. | Windows installer and updates | [features/windows-installer.md](../features/windows-installer.md) |
