.DS_Store
.vscode
.env
.venv
.swp
dist
build
.cache
.gocache
*.exe
.idea
test_data
*.crt
__debug_bin*
llama/build
llama/vendor
/xollama
# `go build .` names its output after the module's last path element, and the
# module path deliberately stays github.com/ollama/ollama -- so a plain build
# still drops an `ollama` binary here. Build with `go build -o xollama .`.
/ollama
integration/testdata/models/

# agent tooling (local state, not part of the fork)
.caliber/
.wolf/

# LSP engine server registry: machine-resolved absolute binary paths.
# Regenerate with claude-hooks/scripts/sync_cclsp.py --write --mcp
cclsp.json
cclsp.json.bak-*
